{
  "scripts": {
    "build": "tsc",
    "start": "node dist/server/index.js",
    "postbuild": "mkdir -p public && cp -rf ../client/dist/* public/"
  },
  "dependencies": {
    "express": "^4.18.2",
    "firebase-admin": "^13.4.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
